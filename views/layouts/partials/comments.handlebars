<div class="comments">
  {{#each this}}
  <section class="comment--section">
    <div class="comment--div">
      {{user.username}} on {{format_date date_created}}
    </div>
    <div class="text">
      {{comment_description}}
    </div>
  </section>
  {{/each}}
</div>