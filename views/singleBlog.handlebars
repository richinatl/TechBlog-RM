{{> post-info post}}

{{#if loggedIn}}
<form class="comment--form">
  <div>
    <textarea class="comment--textarea" name="comment-textarea"></textarea>
  </div>

  <div>
    <button class="btn addcomment--btn" type="submit">Add a comment</button>    
  </div>
</form>
{{/if}}

{{> comments blog.comments}}

{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}}